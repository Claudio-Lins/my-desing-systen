import type { NextPage } from "next";
import Head from "next/head";
import { Activity } from "phosphor-react";

const Home: NextPage = () => {
  return (
    <div className="p-10">
      <h1>Homepage</h1>
    </div>
  );
};

export default Home;
